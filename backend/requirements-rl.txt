# Reinforcement Learning Dependencies for Command Center
# Install with: pip install -r requirements-rl.txt
#
# Note: These packages require significant disk space (~5GB) and GPU support.
# For CPU-only training, remove bitsandbytes (training will be slower).

# PyTorch (install separately for your CUDA version)
# See: https://pytorch.org/get-started/locally/
# torch>=2.1.0

# HuggingFace Transformers
transformers>=4.36.0

# TRL (Transformer Reinforcement Learning)
trl>=0.7.0

# PEFT (Parameter-Efficient Fine-Tuning)
peft>=0.7.0

# BitsAndBytes (quantization support)
# Note: May require CUDA toolkit. Skip for CPU-only.
bitsandbytes>=0.41.0

# HuggingFace Datasets
datasets>=2.14.0

# Accelerate (distributed training)
accelerate>=0.25.0

# Tokenizers
sentencepiece>=0.1.99
tokenizers>=0.15.0

# Optional: For GGUF conversion
# Install llama.cpp separately: git clone https://github.com/ggerganov/llama.cpp && cd llama.cpp && make

# Optional: For async task queue
# django-q>=1.3.9
